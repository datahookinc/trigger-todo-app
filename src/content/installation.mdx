# Installation

**`Trigger`** does not have any dependencies, so installation into your project is simple and lightweight:

```
npm i @datahook/trigger
```

Trigger uses *semver* naming conventions, so you can install a specific version if needed:

```
npm i @datahook/trigger@0.6.0
```

You can review the entire [npmjs version history](https://www.npmjs.com/package/@datahook/trigger?activeTab=versions) for **`Tsrigger`**

---
<div>
    <span style={{ backgroundColor: 'lightyellow' }}>**Note:** It is recommended that you use **`Trigger`** with **`TypeScript`** for an optimal developer experience</span>
</div>